using NUnit.Framework;
using catinc.Controllers;
using catinc.Models.MemberSystem;

namespace Tests.Controllers
{
    [TestFixture]
    public class AccountControllerTests
    {
        private AccountController accountController;

        // [SetUp]
        // public void SetUp()
        // {
        //     accountController = new AccountController(userRepository: new MockUserRepository<MyIdentityUser>(), signInManager: new MockSignInManager<MyIdentityUser>(), emailSender: new MockEmailSender(), logger: new MockLogger());
        // }

        // [test]
    }
}